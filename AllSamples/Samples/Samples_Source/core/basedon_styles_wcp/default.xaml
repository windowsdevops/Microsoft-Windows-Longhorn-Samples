<?Mapping XmlNamespace="media" ClrNamespace="System.Windows.Media" Assembly="PresentationCore"?>
<Border Background="white" ID="root" xmlns="http://schemas.microsoft.com/2003/xaml" xmlns:def="Definition" xmlns:media="media">
<DockPanel>

	<DockPanel.Resources>
	
		
	
                 <Style def:Name="myrect">	
                    <Rectangle 	Fill="LimeGreen" 
						Stroke="Green" StrokeThickness="3" 
						Width="50" Height="200" Margin="10" 
						RadiusX="10" RadiusY="10"
						/> 
                  </Style>

		<Style def:Name="mybluerect" BasedOn="{myrect}">
		     <Rectangle  Fill="blue" /> 
		</Style>	
	</DockPanel.Resources>
	  
	<Rectangle Style="{myrect}"/>
	<Rectangle Style="{mybluerect}" />

	  
	  
	  
	  
</DockPanel>
</Border>